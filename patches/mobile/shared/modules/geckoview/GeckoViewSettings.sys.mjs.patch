diff --git a/mobile/shared/modules/geckoview/GeckoViewSettings.sys.mjs b/mobile/shared/modules/geckoview/GeckoViewSettings.sys.mjs
index 8fb59f0a7f72..21a0cf188289 100644
--- a/mobile/shared/modules/geckoview/GeckoViewSettings.sys.mjs
+++ b/mobile/shared/modules/geckoview/GeckoViewSettings.sys.mjs
@@ -32,6 +32,9 @@ export class GeckoViewSettings extends GeckoViewModule {
     debug`onInit`;
     this._userAgentMode = USER_AGENT_MODE_MOBILE;
     this._userAgentOverride = null;
+    this._platformOverride = null;
+    this._appVersionOverride = null;
+    this._oscpuOverride = null;
     this._sessionContextId = null;
 
     this.registerListener(["GeckoView:GetUserAgent"]);
@@ -79,10 +82,14 @@ export class GeckoViewSettings extends GeckoViewModule {
     this.unsafeSessionContextId = settings.unsafeSessionContextId;
     this.userAgentMode = settings.userAgentMode;
     this.userAgentOverride = settings.userAgentOverride;
+    this.platformOverride = settings.platformOverride ?? null;
+    this.appVersionOverride = settings.appVersionOverride ?? null;
+    this.oscpuOverride = settings.oscpuOverride ?? null;
     this.sessionContextId = settings.sessionContextId;
     this.suspendMediaWhenInactive = settings.suspendMediaWhenInactive;
     this.allowJavascript = settings.allowJavascript;
     this.viewportMode = settings.viewportMode;
+    this.pageZoom = settings.pageZoom;
     this.useTrackingProtection = !!settings.useTrackingProtection;
 
     if (settings.isExtensionPopup) {
@@ -135,6 +142,14 @@ export class GeckoViewSettings extends GeckoViewModule {
       aViewportMode == VIEWPORT_MODE_DESKTOP;
   }
 
+  set pageZoom(aPageZoom) {
+    if (typeof aPageZoom != "number" || aPageZoom <= 0) {
+      return;
+    }
+
+    this.browsingContext.fullZoom = aPageZoom;
+  }
+
   get userAgentMode() {
     return this._userAgentMode;
   }
@@ -163,6 +178,42 @@ export class GeckoViewSettings extends GeckoViewModule {
     this.browsingContext.customUserAgent = this.customUserAgent;
   }
 
+  get platformOverride() {
+    return this._platformOverride;
+  }
+
+  set platformOverride(aPlatform) {
+    if (aPlatform === this.platformOverride) {
+      return;
+    }
+    this._platformOverride = aPlatform;
+    this.browsingContext.customPlatform = aPlatform ?? "";
+  }
+
+  get appVersionOverride() {
+    return this._appVersionOverride;
+  }
+
+  set appVersionOverride(aAppVersion) {
+    if (aAppVersion === this.appVersionOverride) {
+      return;
+    }
+    this._appVersionOverride = aAppVersion;
+    this.browsingContext.customAppVersion = aAppVersion ?? "";
+  }
+
+  get oscpuOverride() {
+    return this._oscpuOverride;
+  }
+
+  set oscpuOverride(aOscpu) {
+    if (aOscpu === this.oscpuOverride) {
+      return;
+    }
+    this._oscpuOverride = aOscpu;
+    this.browsingContext.customOscpu = aOscpu ?? "";
+  }
+
   get suspendMediaWhenInactive() {
     return this.browser.suspendMediaWhenInactive;
   }
